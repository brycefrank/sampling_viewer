// This could just be some d3 visualizations, does it need to be a Phaser scene at all?:


import Phaser from 'phaser';

export default class extends Phaser.scene {
    constructor() {
        super({key:"PopInfo"});
    }
}